experiment:
  name: s1_exp001_byt5_large_fold3
  description: "exp023_full_preprocessingのfold3をbyt5-largeで実行。前処理・学習設定はexp023と同一"

lineage:
  parent: exp023_full_preprocessing
  status: active
  diff_summary: |
    - モデルをgoogle/byt5-base → google/byt5-largeに変更
    - fold3のみ実行
    - その他の設定はexp023と同一

model:
  name: google/byt5-large
  max_length: 512

training:
  seed: 42
  epochs: 20
  lr: 2e-4
  bf16: true
  optimizer: adafactor
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 2
  run_name: s1_exp001_byt5_large_fold3
